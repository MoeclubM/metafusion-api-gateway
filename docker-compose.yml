version: '3.8'

services:
  gateway:
    build: .
    container_name: metafusion-gateway
    restart: unless-stopped
    ports:
      - "10100:80"
    networks:
      - metafusion-net

networks:
  metafusion-net:
    external: true
